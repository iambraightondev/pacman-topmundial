{
  "name": "pacman-topmundial-tests",
  "private": true,
  "scripts": {
    "test:playwright": "playwright test --config=playwright.config.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
